import logging
logging.basicConfig(filename="lb_monitor.log", level=logging.INFO)
logging.info('estos son otros mensajes de info')
logging.error('Y ESTOS SON otros mensjaes de error')
logging.debug('Este es unfffff mensaje de debug')

